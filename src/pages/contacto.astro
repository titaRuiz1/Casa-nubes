---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { Form } from './contacto2';

const lang = 'es';
---

<Layout
  title="Contacto - Casa del Bosque de Nubes"
  description="Contáctanos para reservar tu estadía en Casa del Bosque de Nubes, Gocta"
>
  <Header/>

  <main>
    <!-- Hero Section -->
    <section class="hero-page">
      <div class="hero-content">
        <p class="hero-subtitle">Hemos creado este espacio dedicado a facilitar la comunicación directa con nuestros usuarios. Ya sea que tengas preguntas, comentarios o inquietudes, nos encantaría escucharte. Utiliza el formulario de contacto para enviarnos un mensaje, y te responderemos en el menor tiempo posible. </p>
      </div>
    </section>

    <!-- Contact Form Section -->
    <Form client:load id="contacto" />
  </main>

  <Footer lang={lang} />
</Layout>

<style is:global>
  /* Override header styles for contact page */
  .site-header {
    position: static !important;
    background: #2f210c !important;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  }

  .site-title {
    color: #f5ebe0 !important;
    text-shadow: none !important;
  }

  .menu a {
    color: #f5ebe0 !important;
    text-shadow: none !important;
  }

  .menu a::after {
    background: #d4b827 !important;
  }
</style>

<style>
  .hero-page {
    position: relative;
    min-height: 200px;
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 3em 1rem;
    background: linear-gradient(135deg, #f5ebe0 0%, #ede6cc 50%, #dbdbb8 100%);
  }

  .hero-content {
    position: relative;
    text-align: center;
    max-width: 900px;
    padding: 0 1.5rem;
  }

  .hero-subtitle {
    font-family: Nunito, sans-serif;
    font-size: clamp(1rem, 2.5vw, 1.25rem);
    color: #312e17;
    letter-spacing: 0.03em;
    line-height: 1.9;
    font-weight: 400;
    margin: 0;
  }

  @media (max-width: 768px) {
    .hero-page {
      min-height: 240px;
      padding: 3rem 1rem;
    }
  }
</style>
