---
import { images } from '../../data/images.js';
---

<section id="bienvenidos" class="relative bg-section-brown py-10">
  <div class="welcome-container">
    <!-- Background Image -->
    <div class="welcome-image-wrapper">
      <img
        src={images.welcome.default}
        alt="Bienvenidos a Casa del Bosque de Nubes"
        class="h-full object-cover w-full"
      />
    </div>

    <!-- Floating Text Box -->
    <div class="welcome-text-overlay bg-background-yellow">
        <h2 class="welcome-title">
          BIENVENIDOS
        </h2>

        <!-- Separator -->
        <div class="welcome-separator-container">
          <hr class="welcome-separator-line" />
          <div class="welcome-separator-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="40" height="30" viewBox="0 0 357 357">
              <path fill="currentColor" d="M357,204H0v-51h357V204z"></path>
            </svg>
          </div>
          <hr class="welcome-separator-line" />
        </div>

        <div class="welcome-text">
          <p>
            La Casa del Bosque de Nubes, un refugio custodiado por la catarata de Gocta en medio del bosque nuboso. Despierta en esta montaña Chachapoya, tierra de aventuras y misticismo ancestrales.
          </p>
        </div>

    </div>
  </div>
</section>

<style>
  .welcome-container {
    position: relative;
    width: 100%;
  }

  .welcome-image-wrapper {
    width: 70%;
    height: 700px;
    overflow: hidden;
    margin-left: auto;

    @media (max-width: 768px) {
      width: 100%;
      height: 400px;
    }
  }

  .welcome-image-wrapper img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .welcome-text-overlay {
    position: absolute;
    top: 50%;
    left: 5%;
    transform: translateY(-50%);
    width: 42%;
    max-width: 580px;
    z-index: 10;
    padding: 45px;
  }

  .welcome-box {
    background-color: var(--color-section-beige);
    padding: 2rem 2.5rem;
  }

  .welcome-title {
    color: var(--color-section-dark);
    font-family: 'Nunito', sans-serif;
    font-size: 28px;
    font-weight: 400;
    text-align: center;
    letter-spacing: 0.15em;
    padding: 16px 0px 8px;
    margin: 0;
  }

  .welcome-separator-container {
    text-align: center;
    margin: 12px 0px 20px;
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    gap: 16px;
  }

  .welcome-separator-line {
    flex: 1;
    height: 1px;
    background-color: var(--color-section-white);
    border: none;
    max-width: 150px;
  }

  .welcome-separator-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-section-dark);
    flex-shrink: 0;
  }

  .welcome-separator-icon svg {
    width: 30px;
    height: 22px;
  }

  .welcome-text {
    color: var(--color-section-dark);
    font-family: 'Nunito', sans-serif;
    text-align: center;
    font-weight: 400;
    padding: 0px 32px;
  }

  .welcome-text p {
    margin: 0;
    font-size: 21px;
    line-height: 1.8;
  }

  /* Mobile: Text box below image */
  @media (max-width: 768px) {
    .welcome-container {
      display: flex;
      flex-direction: column;
    }

    .welcome-image-wrapper {
      height: 400px;
      position: relative;
    }

    .welcome-text-overlay {
      position: static;
      transform: none;
      width: 100%;
      max-width: 100%;
    }

    .welcome-box {
      padding: 2rem 1.5rem;
    }

    .welcome-title {
      font-size: 2rem;
    }

    .welcome-text {
      font-size: 0.95rem;
    }

    .welcome-separator-line {
      max-width: 60px;
    }
  }

  @media (max-width: 640px) {
    .welcome-image-wrapper {
      height: 300px;
    }

    .welcome-box {
      padding: 1.5rem 1.25rem;
    }

    .welcome-title {
      font-size: 1.75rem;
    }

    .welcome-text {
      font-size: 0.9rem;
    }
  }
</style>
